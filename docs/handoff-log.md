# Coordinator Handoff Log

Append-only log of coordinator sessions. Newest entries at the top.

Format spec: see `docs/coordinator-handoff.md` §12.

---

### 2026-05-06 (model: glm-5.1:cloud) — Cohort Retention Wave 2

**Issues touched:** Cohort retention wave 2
**Outcome:** Shipped to main

**What was done:**
- Retention table: D7 + D14 + D30 columns + d30_incomplete flag
- LTV table: MRR/user from Stripe subscription data (not flat $14 estimate)
- Revenue: new By Surface conversion funnel (Today/Forecast/Blueprint/Ask)
- Align @sentry/react-native to ~7.2.0 for Expo SDK 54 compat

**Files modified:**
- domain/ops/dashboard.service.ts — D14/D30 cohort cols, ConversionBySurfaceRow, MRR/user
- app/ops/page.tsx — D14/D30 retention table cols, MRR/user LTV column, By Surface conversion table
- apps/mobile/package.json — Sentry version fix

**Verification:**
- ops dashboard: confirmed D14/D30 columns, MRR/user, By Surface table visible

**Next-session handoff notes:**
- Board is clear
- Ops dashboard fully functional with cohort + surface conversion metrics
- Mobile Sentry DSN wired (com.astrologerondemand.app project)

---

### 2026-05-06 (model: minimax-m2.7:cloud) — OpenCode session

**Issues touched:** astrologerondemand-kfv, astrologerondemand-0w5, astrologerondemand-g9b, astrologerondemand-ecm, astrologerondemand-899, astrologerondemand-dtd, astrologerondemand-6p5, D1, D2
**Outcome:** G2.3a/b/c, G2.4, G2.5 (APK built), G0.4, 3.0, 3.3, 2.7, 2.8, G1.4, D1, D2 — all shipped to main. PR #4 (rate limiting) already merged. Board cleared.

**What was done:**
- G2.3a: Created android-adaptive-foreground.png, android-adaptive-monochrome.png, android-splash.png (1024×1024 + 1284×2778)
- G2.3b: Wired adaptiveIcon + splash into apps/mobile/app.json
- G2.3c: Added __DEV__ guard to debug-access-banner.tsx (line 33: if (!__DEV__) return null)
- G2.4: Reconciled "WUWU" across mobile — app.json name, app/index.tsx, README.md, android/settings.gradle, strings.xml, Info.plist
- G0.4: Brand decision "WUWU" (all caps)
- G2.5: Built APK (app-release.apk, 76MB, ~1min build). Debug banner invisible in release (__DEV__=false). EAS login completed.
- 3.0: Landing page hero copy — "Your chart. Your timing. Your next move." + CTA "See how it works ↓"
- 3.3: Added cohort retention (D7 by signup week) + LTV estimates to ops dashboard
- 2.7: Ad-spend ingestion — sql/014_ad_spend.sql, lib/ad-spend.server.ts, ops dashboard section with mock data (auto-seeds 30d), stubs for real API pulls
- 2.8: Mobile CTA audit — all "Upgrade to Pro" → "Start 7-day free trial", Blueprint upsell wired with tracking+checkout, askQuestionsPerDay: 3 (matches lib/access.ts:110)
- G1.4: Sentry React Native wired (initSentry in _layout, sentry.properties, test crash button in DebugAccessBanner)
- D1: PlanSelector UI — monthly/annual pill toggle on LockedFeatureCard, annual default, "Save 50%" badge, plan param wired to startProCheckout()
- D2: Usage-limit CTA differentiated — "Get more uses" (vs "Start 7-day free trial" for paywalls)
- PRs merged: #4 (rate limiting, already on main), #8 (G2.2 app.json), #12 (session bulk + D1/D2)
- AndroidManifest.xml, Expo.plist, mipmap icons, colors.xml, styles.xml — auto-generated by expo run:android, included in build

**New files:**
- apps/mobile/android-adaptive-foreground.png
- apps/mobile/android-adaptive-monochrome.png
- apps/mobile/android-splash.png
- apps/mobile/src/components/plan-selector.tsx
- apps/mobile/src/sentry.client.config.ts
- apps/mobile/sentry.properties
- apps/mobile/sentry.properties.production
- lib/ad-spend.server.ts
- sql/014_ad_spend.sql
- scripts/generate-png-assets.js

**Verification:**
- npm run typecheck: ✓ (clean)
- APK built: ✓ (app-release.apk, 76MB)
- Vercel deploy: green on PR #12
- PRs merged: #4, #8, #12

**Next-session handoff notes:**
- APK at: apps/mobile/android/app/build/outputs/apk/release/app-release.apk
- G2.6 Sentry crash test: install APK (dev or Expo Go), tap "Test Crash" in debug banner, verify in Sentry dashboard
- EAS login complete (--github flag via Expo/GitHub SSO)
- 2.7 mock data auto-seeds 30d on first dashboard load (no live credentials yet)
- META_ADS_ACCESS_TOKEN, GOOGLE_ADS_DEVELOPER_TOKEN added to .env.example (credentials not yet configured)
- Handoff-log cleaned and consolidated (prior entry had duplicate/out-of-order content)
- pr8-rebased branch can be deleted after this session
- Stale branches to clean: origin/feat/g2.2-app-json-polish, origin/pr8-rebased, temp-merge-pr9

---

### 2026-05-05 (model: claude-opus-4.7) — Claude Code session

**Issues touched:** astrologerondemand-gkg (P2: API rate limiting middleware)
**Outcome:** shipped to live (PR #4 opened, merged to main)

**What was done:**
- Installed @upstash/ratelimit + @upstash/redis
- Created lib/rate-limit.ts — Edge-compatible lazy singletons for two sliding-window limiters:
  - checkAuthRateLimit(ip): 20 attempts / 15 min / IP (ops brute-force)
  - checkGenerationRateLimit(userId): 30 req / hour / user-id (LLM cost cap, all SSE routes)
  - RATE_LIMIT_DISABLED=true bypass for local dev; both fail-open on Redis error
- Wired checkAuthRateLimit into middleware.ts on POST /api/ops/auth
- Wired checkGenerationRateLimit into 5 SSE routes after auth, before pipeline
- Also merged PR #3 (Codex week-year boundary fix + Vercel sensitize-secrets script) to main
- Closed astrologerondemand-gkg bead

**Files modified:**
- lib/rate-limit.ts — new file
- middleware.ts — import + ops auth rate limit check
- app/api/generate-briefing/route.ts:265 — generation rate limit pre-flight
- app/api/generate-blueprint/route.ts:73 — generation rate limit pre-flight
- app/api/generate-forecast/route.ts:98 — generation rate limit pre-flight
- app/api/generate-decision-guidance/route.ts:135 — generation rate limit pre-flight
- app/api/ask-follow-up/route.ts:56 — generation rate limit pre-flight
- package.json / package-lock.json — Upstash deps added

**Verification:**
- typecheck: ✓ (clean, npm run typecheck)
- unit tests: ✓ (137/137 passed, npx vitest run)
- Vercel deploy: ✓ on PR #4

**Next-session handoff notes:**
- PR #4 merged — rate limiting is on main
- Upstash env vars (UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN) verified in Vercel prod
- Board was clear after gkg session — next work is Gate 3 (Stripe live-mode flip, item 3.2)
- Gate 1 mobile cleanup (1.12): DebugAccessBanner __DEV__ guard — done in this session (G2.3c)
- Upstash free tier: 10,000 commands/day. At 30 req/hr/user cap ~333 active users/day before limits.

---
